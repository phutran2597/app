const ButtonStyle01 = ({text}) => {
    return (
        <button className="flex border rounded justify-items-center w-40 h-10 items-center justify-center mx-auto bg-main text-white hover:text-main hover:bg-white hover:border-main mt-3">{text}</button>
    )
}

export default ButtonStyle01